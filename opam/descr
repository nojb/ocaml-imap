IMAP4rev1 library for Lwt, Async and Unix

There are two optional dependencies which activate functionality:

* Lwt (with SSL): `opam install ssl lwt imap`
* Async (with SSL): `opam install async_ssl imap`

